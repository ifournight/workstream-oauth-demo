/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable Bun runtime
  experimental: {
    // Bun support
  },
  // Output configuration
  output: 'standalone',
}

export default nextConfig

