/**
 * Shared type definitions for the OAuth verification application
 */

// Breadcrumb types for PageHeader component
export interface BreadcrumbItem {
    label: string;
    href?: string;
}

// Re-export types from other modules for convenience
export type { NavItemType } from "@/components/application/app-navigation/config";

